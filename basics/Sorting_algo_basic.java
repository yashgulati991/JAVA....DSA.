package basics;

public class Sorting_algo_basic {

// BUBBLE SORT 
	/*
	 * public static void bubble_sort(int arr[]) { for (int turn = 0; turn <
	 * arr.length - 1; turn++) { for (int j = 0; j < arr.length - 1 - turn; j++) {
	 * if (arr[j] > arr[j + 1]) { int temp = arr[j]; arr[j] = arr[j + 1]; arr[j = 1]
	 * = temp; } } } }
	 * 
	 * public static void printArr(int arr[]) { for (int i = 0; i < arr.length; i++)
	 * {
	 * 
	 * System.out.print(arr[i] + " "); } System.out.println();
	 * 
	 * }
	 * 
	 * 
	 * // selection sort public static void selection_sort(int arr[]) { int j; for
	 * (j = 0; j < arr.length; j++) { int min = j; int temp = 0; for (int j1 = j +
	 * 1; j1 < arr.length; j1++) { if (arr[j1] < arr[min]) { min = j1; } } temp =
	 * arr[j]; arr[j] = arr[min]; arr[min] = temp; } for (j = 0; j < arr.length;
	 * j++) {
	 * 
	 * System.out.println(arr[j] + " "); } }
	 * 
	 * 
	 */

	public static void main(String[] args) {

		int arr[] = { 5, 4, 1, 3, 2 };
//		bubble_sort(arr);
//		printArr(arr);
		// selection_sort(arr);

	}

}
